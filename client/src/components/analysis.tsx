import React, { useState } from 'react';
import styled from 'styled-components';

const Container = styled.div``;

const Analysis = () => {
    return <Container>Analyzing!</Container>;
};

export default Analysis;
